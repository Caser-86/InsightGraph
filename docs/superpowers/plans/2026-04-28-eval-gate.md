# Eval Gate Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add CI-ready gating options to `insight-graph-eval`.

**Architecture:** Extend `src/insight_graph/eval.py` with JSON case-file loading and a small gate evaluation helper. The CLI writes normal output first, then returns deterministic non-zero exit codes for malformed config or failed gates.

**Tech Stack:** Python argparse, json, pathlib, pytest.

---

## File Structure

- Modify `src/insight_graph/eval.py`: add case-file parser, gate evaluator, CLI arguments.
- Modify `tests/test_eval.py`: add case-file and gate tests.
- Modify `README.md`, `docs/demo.md`, and `CHANGELOG.md`: document eval gate usage.

## Task 1: Failing CLI Gate Tests

- [ ] Add tests for valid `--case-file` loading.
- [ ] Add tests for malformed case file exit code `2`.
- [ ] Add tests for `--min-score` exit code `1`.
- [ ] Add tests for `--fail-on-case-failure` exit code `1`.
- [ ] Add tests proving `--output` writes before returning gate failure.
- [ ] Run `tests/test_eval.py` and confirm failures are due to missing CLI options/helpers.

## Task 2: Case File Loader

- [ ] Implement `load_eval_cases(path)`.
- [ ] Validate top-level object, `cases` list, non-empty `query`, and non-negative integer thresholds.
- [ ] Raise safe `EvalConfigError` for invalid input.
- [ ] Wire `--case-file` to `build_eval_payload()`.

## Task 3: Gate Evaluation

- [ ] Implement `_gate_failures(payload, min_score, fail_on_case_failure)`.
- [ ] Return exit code `1` when gates fail.
- [ ] Return exit code `2` for config errors.
- [ ] Print safe gate/config errors to stderr.
- [ ] Ensure output file/stdout is written before exit code `1` gate failures.

## Task 4: Docs

- [ ] Update README command examples with `--min-score` and `--case-file`.
- [ ] Update demo docs with CI gate example.
- [ ] Update changelog Unreleased entry.

## Task 5: Verification

- [ ] Run `& "C:\Users\77670\AppData\Local\Programs\Python\Python313\python.exe" -m pytest tests/test_eval.py -v`.
- [ ] Run `& "C:\Users\77670\AppData\Local\Programs\Python\Python313\python.exe" -m pytest`.
- [ ] Run `& "C:\Users\77670\AppData\Local\Programs\Python\Python313\python.exe" -m ruff check .`.
- [ ] Run `git diff --check`.

## Self-Review

- Spec coverage: case-file loading, threshold gates, output ordering, safe errors, docs, and verification are covered.
- Placeholder scan: no placeholders.
- Type consistency: `EvalConfigError`, `load_eval_cases`, and gate arguments match the spec.
